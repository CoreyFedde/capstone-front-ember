{{page-header title="Loans"}}
<div class="container">
  <div class="col-md-12">
    <div class="row">
      <div class="col-md-6">
        <h3>Your View</h3>
        {{loans/drop-down loans=model createLoan="createLoan" selectLoan="selectLoan"}}

        {{!-- {{#if (eq selectloan.value "new-loan")}}
        <h1>create new</h1>
        {{else}}
        <h1>tracked loan</h1>
        {{/if}}

        {{#if selectloan}}
        <h1>Has value</h1>
        {{else}}
        <h1>no value</h1>
        {{/if}} --}}
        {{!-- {{loan-list/create-loan-form createLoan="createLoan"}} --}}
      </div>
      <div class="col-md-6">
        <h3>Tools</h3>
        <h5>360 Loan Review</h5>
        <h5>Loan Visualization</h5>
      </div>
    </div>
  </div>
</div>
